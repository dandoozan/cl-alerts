import React from 'react';
import SuccessPage from './SuccessPage';
import { render, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

//tests to make:
//  -should render with correct values
//  -should redirect to homepage if visited directly
//  -should

it('should have my element', async () => {
  // let { findByTestId } = render(<SuccessPage />);
  // expect(await findByTestId('my-element')).toBeInTheDocument();
});
