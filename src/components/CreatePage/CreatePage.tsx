import React from 'react';
import styles from './CreatePage.module.css';

interface Props {}

export default function CreatePage({ }: Props) {
    return (<div className={styles.createPage}>CreatePage</div>);
};
